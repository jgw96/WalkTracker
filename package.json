{
  "name": "walktracker",
  "private": true,
  "version": "0.0.1",
  "description": "walktracker",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.0.0-beta.19",
    "idb-keyval": "^3.1.0"
  },
  "devDependencies": {
    "@stencil/core": "^0.16.1",
    "workbox-build": "3.4.1"
  }
}
